// import React from 'react';
// import PropTypes from 'prop-types';

const Statistic = () => {
    return (
        <div>
            <h1>Statistic</h1>
        </div>
    );
};

Statistic.propTypes = {
    
};

export default Statistic;